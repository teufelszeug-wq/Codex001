export default function SettingsPage() {
  return (
    <main className="shell narrow">
      <p className="eyebrow">SETTINGS</p>
      <h1>設定</h1>
      <p>M1では設定画面の入口のみを固定します。保存先、Lint、AI、言語パック等は各マイルストーンで追加します。</p>
    </main>
  );
}
